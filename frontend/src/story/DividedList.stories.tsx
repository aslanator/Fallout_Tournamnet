import React from 'react';

import { Story, Meta } from '@storybook/react/types-6-0';

import {DividedList, DividedListProps} from 'components/lists/DividedList/DividedList';

export default {
  title: 'Components/DividedList',
  component: DividedList,
} as Meta;

// const Template: Story<DividedListProps> = (args) => <DividedList {...args} />

// export const Default = Template.bind({});
// Default.args = {
// }